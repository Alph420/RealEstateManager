<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- base application theme. -->
    <style name="BaseTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/green_money_twice</item>
        <item name="colorSecondary">@color/purple_money_twice</item>
        <item name="toolbarStyle">@style/Widget.MaterialComponents.Toolbar.Surface</item>
        <item name="colorControlActivated">@color/green_money_twice</item>
        <item name="colorInteractiveIcon">@color/purple_money_twice</item>
    </style>


    <!-- light mode -->
    <style name="AppTheme" parent="BaseTheme">
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
        <item name="android:statusBarColor">@color/white</item>
        <item name="green">@color/green_money_twice</item>
    </style>

    <!-- This style is where you define the tint color of your overflow menu button -->
    <style name="actionOverflowButtonStyle" parent="@style/Widget.AppCompat.ActionButton.Overflow">
        <item name="android:tint">@color/green_money_twice</item>
    </style>

</resources>
