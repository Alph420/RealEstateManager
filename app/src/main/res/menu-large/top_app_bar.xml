<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:ignore="MissingDefaultResource">
    <item
        android:id="@+id/searchItem"
        android:icon="@drawable/ic_search"
        tools:iconTint="@color/white"
        android:title="@string/search_action_icon"        app:showAsAction="always" />
    <item
        android:id="@+id/addItem"
        android:icon="@drawable/ic_add"
        tools:iconTint="@color/white"
        android:title="@string/add_action_icon"
        app:showAsAction="always" />

    <item
        android:id="@+id/mapItem"
        android:icon="@drawable/ic_near"
        android:title="@string/map_action_icon"
        app:showAsAction="always"/>

    <item
        android:id="@+id/simulator"
        android:icon="@drawable/ic_simulator"
        tools:iconTint="@color/white"
        android:title="@string/simulator_action_icon"
        app:showAsAction="always"/>
    <item
        android:id="@+id/settings"
        android:icon="@drawable/ic_settings"
        tools:iconTint="@color/white"
        android:title="@string/settings_action_icon"
        app:showAsAction="always" />
</menu>